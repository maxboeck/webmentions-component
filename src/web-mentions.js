import { LitElement, html } from 'lit-element'

class WebMentions extends LitElement {
    render() {
        return html`
            <div>The Component Template</div>
        `
    }
}

customElements.define('web-mentions', WebMentions)
